name: step_alarm
description: "A Flutter Step Alarm application."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  alarm: ^3.1.2
  pedometer: ^4.0.1
  hive_flutter: ^1.1.0
  provider: ^6.1.2
  permission_handler: ^11.3.1
  google_mobile_ads: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.1
  build_runner: ^2.4.9

flutter:
  uses-material-design: true
